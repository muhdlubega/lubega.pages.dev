import App from "./App";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")).render(<App />);

const main = () => {
  return <div>main</div>;
};

export default main;
